#include <stdint.h>
#include <stdlib.h>

#include "globals.h"

#ifndef GAME_BOARD_H
#define GAME_BOARD_H

// Generates the entire board

/**
 * Perhaps we can have several pre-defined tracks? (3??)
 * One is an easy track, one is a medium track, and one is a hard track??
 * 
 * Perhaps we want to give it a 2.5d view or something when we are turning
 */

#endif
